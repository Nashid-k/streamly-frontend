/* ── Player UI shared definitions ────────────────────────────────────────
   The player chrome is a single fixed Netflix-style design (black +
   #E50914). PLAYER_SPEEDS is the only shared definition left — the
   Player UI Studio (presets / skins / zones / icon variants) was removed. */

export const PLAYER_SPEEDS = [0.5, 0.75, 1, 1.25, 1.5, 2];